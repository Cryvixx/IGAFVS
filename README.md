# iGAFVS - Interactive Geometric Analysis and Function Visualization Studio

**iGAFVS** - это мощный инструмент для визуализации математических функций и построения геометрических фигур на одном холсте.

## 📋 Содержание
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Инструменты](#инструменты)
- [Горячие клавиши](#горячие-клавиши)
- [Архитектура кода](#архитектура-кода)
- [Сохранение проектов](#сохранение-проектов)
- [Примеры использования](#примеры-использования)

---

## 🚀 Установка

### Требования:
- Python 3.8 или выше
- PyQt5
- NumPy
- SymPy

### Установка зависимостей: 
```bash
pip install PyQt5 numpy sympy
```

### Запуск программы: 
```bash
python main_window.py
```

---

## 🎯 Быстрый старт

### Первый запуск:
1. Запустите программу командой выше
2. Вы увидите белый холст с сеткой
3. Выберите инструмент из панели сверху (кнопки с иконками)
4. Начните рисовать на холсте! 

### Основные действия: 
- **Левый клик** - выполняет действие выбранного инструмента
- **Правый клик** - удаляет объект под курсором
- **Средняя кнопка** - панорамирование (перемещение холста)
- **Колесо мыши** - зум (приближение/удаление)
- **Пробел** - временная активация режима панорамирования (❌ Не работает в актуальной версии)

---

## 🛠️ Инструменты

### 1. **Select (Выбор)** - кнопка со стрелкой
Инструмент по умолчанию. Используется для выбора элементов.

### 2. **Point (Точка)** - кнопка с точкой
Добавляет одиночные точки на холст.

**Как использовать:**
1. Нажмите кнопку "Point" на панели
2. Кликните на холст где хотите поставить точку
3. Точка автоматически прилипнет к: 
   - Графикам функций
   - Пересечениям функций
   - Другим точкам
   - Сторонам фигур

### 3. **Line (Линия)** - кнопка с линией
Рисует линии между двумя точками. 

**Как использовать:**
1. Выберите инструмент "Line"
2. Кликните в первую точку (начало линии)
3. Кликните во вторую точку (конец линии)
4. Если линия не соединена с другими объектами, она будет бесконечной (пунктирная)

### 4. **Circle (Окружность)** - кнопка с кругом
Рисует окружности. 

**Как использовать:**
1. Выберите инструмент "Circle"
2. Кликните в центр окружности
3. Кликните на краю (радиус окружности будет вычислен автоматически)

**💡 Совет:** Центр окружности прилипает к точкам, поэтому можно строить окружности точно от других точек! 

### 5. **Polygon (Многоугольник)** - кнопка с треугольником
Рисует многоугольники (фигуры с несколькими сторонами).

**Как использовать:**
1. Выберите инструмент "Polygon"
2. Левый клик - добавить вершину многоугольника
3. Повторяйте клики для добавления вершин
4. Правый клик на первую вершину - завершить построение
5. Или нажмите ESC для отмены

### 6. **Angle (Угол)** - кнопка с углом
Строит угол между тремя точками.

**Как использовать:**
1. Выберите инструмент "Angle"
2. Кликните первую точку (один луч угла)
3. Кликните вторую точку (ВЕРШИНА угла - центральная точка)
4. Кликните третью точку (второй луч угла)
5. Введите величину угла в градусах
6. Вторая линия автоматически повернётся на нужный угол

**Пример:** Если вы построили угол и ввели 90°, то между лучами будет прямой угол.

### 7. **Text (Текст)** - кнопка с буквой T
Добавляет текст на холст.

**Как использовать:**
1. Выберите инструмент "Text"
2. Кликните где хотите добавить текст
3. Введите текст в диалоговое окно
4. Нажмите Enter

---

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **Пробел** | Активировать режим панорамирования (удерживать) | (❌ Не работает в актуальной версии)
| **ESC** | Отменить текущее построение (для многоугольника, угла) |
| **Правый клик** | Удалить объект под курсором |
| **Колесо вверх** | Приблизить (zoom in) |
| **Колесо вниз** | Отдалить (zoom out) |

---

## 📊 Функции (Графики)

### Добавление функций: 
1. В поле "y =" введите математическое выражение
2. Нажмите кнопку "Добавить"
3. График функции появится на холсте в одном из 8 предопределённых цветов

### Поддерживаемые функции:
```
sin(x)      - синус
cos(x)      - косинус
tan(x)      - тангенс
cot(x)      - котангенс (или ctg(x))
sqrt(x)     - квадратный корень
abs(x)      - модуль (абсолютное значение)
x^2         - возведение в степень (используйте ^)
1/x         - деление
x + 2       - сложение
x - 3       - вычитание
2 * x       - умножение (или просто 2x)
pi          - число π (3.14159...)
e           - число e (2.71828...)
2           - константы (числа без x)
```

### Примеры функций:
```
sin(x)                  ✅ Синусоида
x^2 - 2*x + 1          ✅ Парабола
1/x                     ✅ Гипербола
sqrt(4 - x^2)          ✅ Полуокружность
sin(x) + cos(x)        ✅ Сумма функций
pi                      ✅ Горизонтальная линия y = π
```

### Управление функциями:
- **✓ кнопка** - включить/выключить видимость функции
- **✕ кнопка** - удалить функцию из списка

---

## 💾 Сохранение проектов

### Сохранение: 
1. Нажмите кнопку **"Save"** на панели инструментов
2. Введите имя файла (расширение . IGAFVS добавится автоматически)
3. Нажмите Enter
4. Файл сохранится в текущей папке

### Загрузка:
1. Нажмите кнопку **"Load"** на панели инструментов
2. Введите имя файла (без расширения или с . IGAFVS)
3. Нажмите Enter
4. Проект загрузится со всеми объектами и функциями

### Что сохраняется:
- ✅ Все графики функций и их состояние (видны/скрыты)
- ✅ Вcе нарисованные линии, окружности, многоугольники, углы
- ✅ Все добавленные точки
- ✅ Все текстовые элементы
- ❌ Позиция камеры и уровень зума (ВНИМАНИЕ:  не сохраняется!)

---

## 🏗️ Архитектура кода

### Структура проекта:
```
project/
├── main_window.py           # Главное окно и холст для рисования
├── hover_toolbar.py         # Панель инструментов и ввод функций
├── drawing_objects.py       # Методы рисования геометрических фигур
├── icons/                   # Папка с иконками
│   ├── select. png
│   ├── point.png
│   ├── line.png
│   ├── circle.png
│   ├── polygon.png
│   ├── angle.png
│   ├── text.png
│   ├── grid.png
│   ├── save.png
│   └── import.png
└── README.md               # Этот файл
```

### Главные классы: 

#### **DrawingCanvas** (`main_window.py`)
Холст для рисования.  Содержит:
- Все объекты (линии, окружности, точки, углы, тексты)
- Все функции
- Логика интерактивности (мышь, клавиатура)
- Система координат (преобразование экранных координат в мировые)
- Система прилипания (snap)

**Главные методы:**
- `mousePressEvent()` - обработка клика мыши
- `mouseMoveEvent()` - обработка движения мыши
- `wheelEvent()` - обработка зума
- `paintEvent()` - рисование всего на экране
- `find_snap_point()` - поиск ближайшей точки для прилипания
- `world_to_screen()` - преобразование мировых координат в экранные
- `screen_to_world()` - преобразование экранных координат в мировые

#### **HoverToolbar** (`hover_toolbar.py`)
Панель инструментов.  Содержит:
- Кнопки выбора инструментов
- Поле ввода функций
- Список добавленных функций
- Кнопки сохранения/загрузки

#### **DrawingObjects** (`drawing_objects.py`)
Статический класс с методами для рисования:
- `draw_point()` - рисует точку
- `draw_line()` - рисует линию
- `draw_circle()` - рисует окружность
- `draw_polygon()` - рисует многоугольник
- `draw_angle()` - рисует угол

#### **MainWindow** (`main_window.py`)
Главное окно приложения. Обрабатывает:
- События из панели инструментов
- Сохранение и загрузку проектов
- Горячие клавиши

---

## 🧮 Как работает координатная система

### Преобразование координат: 

iGAFVS использует две системы координат:

1. **Экранные координаты (Screen)** - пиксели на мониторе (0,0 в левом верхнем углу)
2. **Мировые координаты (World)** - математическая система (0,0 в центре, как на графиках)

### Преобразование: 
```python
# Из мировых в экранные
screen_x = center_x + world_x * grid_size
screen_y = center_y - world_y * grid_size  # Минус потому что Y смотрит вверх

# Из экранных в мировые
world_x = (screen_x - center_x) / grid_size
world_y = (center_y - screen_y) / grid_size
```

Где:
- `center_x, center_y` - центр экрана
- `grid_size` - размер одного квадрата сетки (зависит от зума)
- `offset_x, offset_y` - смещение камеры

---

## 🎮 Система прилипания (Snap)

iGAFVS автоматически "прилипает" к важным точкам:

### Точка прилипается к:
1. ✅ Графикам функций
2. ✅ Пересечениям функций
3. ✅ Другим добавленным точкам
4. ✅ Центрам окружностей
5. ✅ Сторонам линий и многоугольников (только для инструмента Point)

### Как это работает:
1. Когда вы перемещаете мышь, программа ищет ближайшую важную точку
2. Если расстояние меньше `snap_radius` (15 пикселей), появляется жёлтый круг
3. Когда вы кликаете, точка прилипает туда где жёлтый круг

### Радиус прилипания:
```python
snap_radius = 15  # В пикселей
snap_range = snap_radius / grid_size  # В мировых координатах
```

---

## 📝 Примеры использования

### Пример 1: Построение точек пересечения функций

```
1. Добавьте функцию: y = x^2
2. Добавьте функцию: y = x + 2
3. Выберите инструмент "Point"
4. Кликните в местах пересечения графиков (жёлтый круг покажет)
5. Точки добавятся ровно в местах пересечения
```

### Пример 2: Построение треугольника

```
1. Выберите инструмент "Polygon"
2. Кликните 3 раза для создания вершин треугольника
3. Правый клик на первую вершину для завершения
4. Треугольник нарисуется с чёрными сторонами
```

### Пример 3: Построение угла 45°

```
1. Выберите инструмент "Angle"
2. Кликните точку на одном луче
3. Кликните вершину угла (центральную точку)
4. Кликните на другой луч
5. В диалоговом окне введите:  45
6. Второй луч повернётся на 45° от первого луча
```

### Пример 4: Анализ функции через точки

```
1. Добавьте функцию: y = sin(x)
2. Выберите инструмент "Point"
3. Кликайте на графике в интересующих вас местах
4. Точки прилипнут точно к графику
5. Координаты видны в нижнем правом углу при наведении
```

---

## 🔧 Расширение и модификация

### Добавление нового инструмента: 

1. **Добавьте кнопку в `hover_toolbar. py`:**
```python
buttons = [
    # ... существующие кнопки ... 
    ("mytool", "icons/mytool.png"),  # ← Новая кнопка
]
```

2. **Добавьте обработку в `main_window.py`:**
```python
elif self.current_tool == 'mytool':
    # Ваша логика построения инструмента
    pass
```

3. **Добавьте иконку `mytool. png` в папку `icons/`**

### Добавление новой функции для графиков:

1. **В `main_window.py` в методе `_process_function()`:**
```python
func = lambdify(self.x, expr, modules=[{
    'sin': np.sin,
    'myfunction': my_custom_function,  # ← Добавить
    # ... остальные функции ...
}, 'numpy'])
```

2. **Определите вашу функцию:**
```python
def my_custom_function(x):
    return x**3 - 2*x
```

---

## 🐛 Частые ошибки и решения

### "Некорректная функция"
**Причина:** Неправильный синтаксис функции
**Решение:** Проверьте: 
- Скобки парные?  `sin(x)` ✅, `sin(x` ❌
- Используете `^` для степени? `x^2` ✅, `x**2` ❌
- Переменная `x` есть в формуле? `x + 2` ✅, `y + 2` ❌

### Точка не прилипает к графику
**Причина:** Точка слишком далеко от графика
**Решение:**
- Приблизьтесь (zoom in) для точности
- Кликайте точно на график

### Окружность построилась неправильно
**Причина:** Может быть, вы начали не с центра
**Решение:**
- Удалите окружность (правый клик)
- Выберите инструмент Circle заново
- Первый клик = центр, второй = край

---

## 📚 Дополнительные ресурсы

### Документация: 
- [PyQt5 документация](https://doc.qt.io/qt-5/)
- [NumPy документация](https://numpy.org/doc/)
- [SymPy документация](https://docs.sympy.org/)

---

## 📄 Лицензия

Этот проект создан в образовательных целях. 

---

## 👨‍💻 Автор

Создано как учебный проект для изучения: 
- PyQt5 (графический интерфейс)
- Математических вычислений (SymPy, NumPy)
- Системы координат и трансформаций
- Интерактивных приложений

---

**Версия:** 1.0
**Дата обновления:** 2026-01-11
**Статус:** Полностью функциональная версия ✅